---
category: Primitives
---

Spinner plus label, `role="status"`. Use for in-panel waits; use skeletons only where the shape is already known.

```tsx
<LoadingState label="Preparing secure call…" />
```

`label` defaults to `Loading…`.
