.env
.secrets
.DS_Store
node_modules/
dist/
coverage/
*.log
.idea/
.vscode/
cookies*.txt
cookie*.txt

# OpenTofu / Terraform
terraform/.ssh/
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
terraform/.terraform.lock.hcl

# Hetzner deployment secrets
deploy/hetzner/*/.env

# design-sync (staged scripts + regenerated output + machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
app/web/types/

# Agent tooling. Claude Code is the only agent this repo targets, so .claude/ is
# the single tracked home for skills and shared settings. Per-tool mirrors of the
# same install (.agents/ for Devin/Codex used to exist and drifted) stay out, as
# does machine-local state.
.agents/
.codex/
.opencode/
.impeccable/
.claude/settings.local.json
