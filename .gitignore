.env
.secrets
.DS_Store
node_modules/
dist/
coverage/
*.log
.idea/
.vscode/
cookies*.txt
cookie*.txt

# OpenTofu / Terraform
terraform/.ssh/
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
terraform/.terraform.lock.hcl

# Hetzner deployment secrets
deploy/hetzner/*/.env

# design-sync (staged scripts + regenerated output + machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
app/web/types/

# Agent tooling. The impeccable skill is tracked under .agents/skills/ and
# .claude/skills/, since each tool resolves its own copy. Further per-tool
# mirrors of the same install, and machine-local state, stay out.
.codex/
.opencode/
.impeccable/
.claude/settings.local.json
