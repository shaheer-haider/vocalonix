# Agent instructions

**See [`CLAUDE.md`](CLAUDE.md).**

Claude Code is the agent this repository targets, and `CLAUDE.md` is its single
entry point: stack, commands, repository map, invariants, definition of done,
and the landmines. Repo-local skills live in [`.claude/skills/`](.claude/skills/).

This file is a pointer and nothing else. Two files describing the same rules is
how they drifted apart last time.
