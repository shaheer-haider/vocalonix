{
    email hello@harkbell.com
}

# Not proxied by Cloudflare (the A record points straight at this box), so
# unlike the vocalonix origin this one gets a normal Let's Encrypt certificate
# through the automatic HTTP-01 challenge.
voice.harkbell.com {
    reverse_proxy /api/v1/* api:8000
    reverse_proxy /voice-audio/* minio:9000
    reverse_proxy /* ui:3010
}
